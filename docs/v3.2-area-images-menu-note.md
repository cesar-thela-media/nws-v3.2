# Area image/menu update

- Added explicit hero image and alt associations to all eight linked service-area records.
- Richmond remains base context without an invented route.
- Area hero assets are generic NWS project imagery unless the client source supplies an area-specific asset; alt text deliberately says `serving <area>` and does not claim the project is located there.
- Reorganized the desktop Areas dropdown into two columns.
- Mobile Areas navigation remains a single accessible list and retains all eight linked routes.
- Browser verification: desktop and mobile area links present, two desktop columns, no horizontal overflow.
- Note: the first unique-image structural check identified duplicate associations in the working tree; the final mapping was corrected so each linked area uses a distinct local image.
